<% provide(:title, "Start a new conversation") %>
<h1>Start a new conversation</h1>

<%= form_tag conversations_path, method: :post do %>
  <div>
    <%= select_tag :user_id, options_from_collection_for_select(@recipients, :id, :username) %>
  </div>

  <div>
    <%= text_field_tag :subject, nil, placeholder: "Subject" %>
  </div>

  <div>
    <%= text_area_tag :body, nil, placeholder: "Write your message here..." %>
  </div>

  <%= submit_tag %>
<% end %>
